#include<stdio.h>

int main (){

// variable = A reusable container for a value.
// Behaves as if it were value it contains.

    float gpa = 4.0;
    float price = 19.99;
    float temperature = -10.5;

    printf("My GPA is %f \n", gpa);
    printf("This price is $%f\n", price);
    printf("Outside temperature is %f\n", temperature);

    return 0;
}