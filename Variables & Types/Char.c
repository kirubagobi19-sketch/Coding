#include<stdio.h>
int main (){

// variable = A reusable container for a value.
// Behaves as if it were value it contains.

char grade = 'A';
char symbol = '!';
char currency = '$';

printf("Your grade is %c\n", grade);
printf("My favourite symbol is %c\n", symbol);
printf("American currency is %c\n", currency);

return 0;
}